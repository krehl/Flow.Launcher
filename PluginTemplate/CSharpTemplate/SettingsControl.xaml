<UserControl x:Class="Flow.Launcher.Plugin.PluginTemplate.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel Margin="10">
        <TextBlock Text="Plugin Template Settings" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <!-- Example boolean setting -->
        <CheckBox Content="Enable Example Feature" 
                  IsChecked="{Binding EnableExampleFeature}" 
                  Margin="0,0,0,10"/>
        
        <!-- Example string setting -->
        <TextBlock Text="Example Text:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding ExampleText}" Margin="0,0,0,10"/>
        
        <!-- Example integer setting -->
        <TextBlock Text="Maximum Results:" Margin="0,0,0,5"/>
        <TextBox Text="{Binding MaxResults}" Margin="0,0,0,20"/>
        
        <!-- Save button -->
        <Button Content="Save Settings" Click="SaveButton_Click" Width="120" HorizontalAlignment="Left"/>
    </StackPanel>
</UserControl>
